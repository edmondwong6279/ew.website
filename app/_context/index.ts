/**
 * This directory contains the context that is required. At the moment,
 * we only need context to know if the front end should render light
 * mode or dark mode.
 */

export * from "./themeContext";
